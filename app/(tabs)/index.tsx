import { TabScreen } from '@/features/shell/TabScreen';

export default function TodayScreen() {
  return <TabScreen eyebrow="Today" title="What should we brew?" description="Quick start from recent recipes, favorite brews, and active experiments." />;
}
